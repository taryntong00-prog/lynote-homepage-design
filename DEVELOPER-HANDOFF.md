# Lynote 首页单排版 · 开发交付

## 入口

- 最新设计：双击 `single-row.html`，无需安装依赖或构建。
- 在线预览：https://taryntong00-prog.github.io/lynote-homepage-design/single-row.html
- `index.html` 是保留的旧双排对比稿，不是本次实施入口。

## 文件与集成

- `single-row.html`：最新首页结构。
- `styles.css`：基础首页样式，需先加载。
- `single-row.css`：单排背书与独立媒体横条样式，需在基础样式后加载。
- `single-row.js`：媒体轮播、暂停按钮、示例文本、模式切换和字数统计。
- `assets/`：本地品牌、徽章及媒体 Logo 素材。保持相对路径。
- `index.html` / `script.js`：旧双排版本，仅供对照。

首屏 `.hero-area` 内的 `.single-proof` 是静态背书条，顺序为 **1M+ users → 4.9 / 5 → Product Hunt → Trendshift Python #3**。桌面居中排列；窄屏可手动横向滚动。

首屏之外的 `.media-transition` 是独立白底媒体横条，桌面高 84px、手机高 76px。11 个灰度 Logo 缓慢循环，支持悬停、聚焦和按钮暂停。系统启用减少动态效果时停止动画，改为手动横向查看。集成完整首页时，应将它放在后续产品内容之前，不合并回浅紫首屏。

轮播脚本只克隆一组 Logo，克隆组对辅助技术隐藏。集成到组件框架时请避免重复初始化；如使用全站基础样式，需要迁移对应选择器及动画，避免直接覆盖全局样式。

## 上线前确认

- 当前为交互设计稿，没有 AI 后端；主按钮跳到官网，不提交编辑框内容。
- 评分与用户数为设计稿所用数据，正式上线须由业务确认来源、统计口径及有效性。
- Trendshift 是 2026-09-15 的 Python 分类日榜第 3 名，不是全语言榜第三，也不代表当前排名。
- Product Hunt 徽章是静态快照，票数需在上线时确认或替换为官方动态徽章。
- Google Fonts 的 Inter 字体需要网络；断网会回退到系统字体。图片、样式和脚本均在包内。
- 可移除页面底部 `design-note` 对比预览说明；导航与工具按钮请接正式业务路由。

本次交付未覆盖原双排页面，也未修改线上 Lynote 官网。
